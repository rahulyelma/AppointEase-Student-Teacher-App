rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write access for now for development.
      // IMPORTANT: Change this to more restrictive rules in production!
      allow read, write: if true;
    }
  }
}